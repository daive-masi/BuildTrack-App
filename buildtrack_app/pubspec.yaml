name: buildtrack_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.1 <4.0.0'  # CORRIGÉ

dependencies:
  flutter:
    sdk: flutter

  # FIREBASE CORE - AVEC VERSIONS EXPLICITES
  firebase_core: ^2.24.0
  firebase_auth: ^4.14.0
  cloud_firestore: ^4.13.0

  # AUTHENTIFICATION - AVEC VERSIONS EXPLICITES
  google_sign_in: ^6.1.5
  # sign_in_with_apple: ^5.0.0  # OPTIONNEL POUR L'INSTANT

  # UI & NAVIGATION - AVEC VERSIONS EXPLICITES
  provider: ^6.1.1
  # flutter_svg: ^2.0.7  # OPTIONNEL POUR L'INSTANT
  qr_flutter:

  # STORAGE & UTILS - AVEC VERSIONS EXPLICITES
  shared_preferences: ^2.2.2
  # crypto: ^3.0.3  # OPTIONNEL POUR L'INSTANT

  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true