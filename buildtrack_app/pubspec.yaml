name: buildtrack_app
description: "Une application Flutter pour le suivi des chantiers et des tâches."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.1 <4.0.0'
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

  # 🔥 FIREBASE CORE - AVEC VERSIONS STABLES
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.0.0
  firebase_app_check: ^0.3.1

  # 🔒 AUTHENTIFICATION
  google_sign_in: ^6.1.5

  # 📱 UI & NAVIGATION
  provider: ^6.1.1
  cupertino_icons: ^1.0.8
  mobile_scanner: ^4.0.0  # Scanner QR
  permission_handler: ^11.0.1  # Gestion des permissions
  intl: ^0.20.2  # Internationalisation

  # 📊 GRAPHIQUES
  fl_chart: ^0.68.0  # ⭐ NOUVEAU: Graphiques animés

  # 📍 GÉOLOCALISATION & IMAGES
  geolocator: ^11.0.1
  image_picker: ^1.2.0

  # 📝 TÂCHES & INTERACTIONS
  drag_and_drop_lists: ^0.4.2 # ⭐ NOUVEAU: Drag & drop des tâches

  # 🔔 NOTIFICATIONS
  flutter_local_notifications: ^17.1.2  # ⭐ NOUVEAU: Notifications locales

  # 📄 STORAGE & UTILS
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
