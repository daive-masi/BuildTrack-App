name: buildtrack_app
description: "Une application Flutter pour le suivi des chantiers et des tâches."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.6.1 <4.0.0'
  flutter: ">=3.27.0"

dependencies:
  flutter:
    sdk: flutter

  # 🌍 LANGUES & TRADUCTIONS
  flutter_localizations:
    sdk: flutter
  # CORRECTION ICI : Version forcée par Flutter
  intl: ^0.20.2

  # 🔥 FIREBASE CORE
  firebase_core: ^3.0.0
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.0.0
  firebase_app_check: ^0.3.1

  # 🔒 AUTHENTIFICATION
  google_sign_in: ^6.1.5

  # 📱 UI & NAVIGATION
  provider: ^6.1.1
  cupertino_icons: ^1.0.8
  mobile_scanner: ^5.0.0
  permission_handler: ^11.0.1
  url_launcher: ^6.2.5

  # 📊 GRAPHIQUES
  fl_chart: ^0.68.0

  # 📍 GÉOLOCALISATION & IMAGES
  geolocator: ^11.0.1
  image_picker: ^1.0.7

  # 📝 TÂCHES & INTERACTIONS
  drag_and_drop_lists: ^0.4.2

  # 🔔 NOTIFICATIONS
  flutter_local_notifications: ^17.1.2

  # 📄 STORAGE & UTILS
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true